import ProductWalletWrapper from "@/app/components/customerDashboard/privilegeDashboard/product-wallet/ProductWalletWrapper";

const ProductWalletPage = ({ searchParams }) => {
    return <ProductWalletWrapper searchParams={searchParams} />;
};

export default ProductWalletPage;
